/*!
 * Copyright (C) 2018-2020  Zachary Kohnen (DusterTheFirst)
 */

import styled from "styled-components/native";

/** A view that uses flex */
export const FlexView = styled.View({
    flex: 1
});

/** A keyboard avoiding view that uses flex */
export const FlexKeyboardAvoidingView = styled.KeyboardAvoidingView({
    flex: 1
});